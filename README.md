KissanConnect - Final Static Site (Schemes, Crop Disease, Soil Support, Chatbot)

How to publish:
1. Create a GitHub repository named `kissanconnect` (or use your repo).
2. Upload all files from this ZIP to the repository root (index.html, styles.css, script.js, SVG files).
3. In GitHub repo settings → Pages: Branch = main, Folder = / (root). Save.
4. Visit: https://<your-username>.github.io/kissanconnect/

Notes:
- All assets are in the repo root (logo.svg, hero.svg, turmeric.svg, wheat.svg, rice.svg, market.svg, weather.svg, security.svg).
- Chatbot is client-side demo. To connect to real AI, add a backend and secure API keys.
